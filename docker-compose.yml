version: '3.8'
services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1729
    ports:
      - 5433:5432
    volumes:
      - ./data:/var/lib/postgresql/data